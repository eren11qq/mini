# deps
node_modules/

# build (v1 无构建,但保留以备)
dist/
*.tsbuildinfo

# test artifacts
coverage/

# secrets — 永不进库
.env
.env.*
!.env.example

# logs / OS
*.log
.DS_Store

# session data lives in ~/.mini/, not repo — 若误落此处也忽略
.mini/

# Claude Code 个人权限 / 本地覆盖 — 不进库
.claude/settings.local.json
