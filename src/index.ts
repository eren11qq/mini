// mini — pi 式编码 agent (v1 复刻)
// 本文件为占位入口。真模块按 PRD 建造顺序 ⓪→④ 落入 src/{loop,stream,tools,memory,harness}/。
// 反馈循环(tsc + vitest + prettier + husky)先于业务码就位,以便后续 AI 交付即受校验。

export const MINI_VERSION = "0.1.0";
